#include <string.h>
#include <stdio.h>
#include <stdlib.h>

#define TRUE 1
#define FALSE 0


int main(void){
  int linha, coluna;
  node *head = (node *)malloc(sizeof(node));
  node *n = head;
  int peca;
  FILE *fptr;

  
  fptr = fopen("jogos.dat", "wb+");

  tabuleiro()
  
  return 1;
}





